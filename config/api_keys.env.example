# config/api_keys.env.example
# Copy this to api_keys.env and add your actual keys

# Alpha Vantage (free tier: 5 calls/min, 500 calls/day)
ALPHA_VANTAGE_API_KEY=api_keys.env

# Google Sheets (for automation)
GOOGLE_SHEETS_CREDENTIALS_PATH=config/creds.json

